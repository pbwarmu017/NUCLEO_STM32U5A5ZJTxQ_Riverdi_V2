# Build outputs (regenerated by every build)
Debug/
Release/
*.o
*.d
*.elf
*.bin
*.hex
*.list
*.map
*.su

# Eclipse / CubeIDE IDE state (user-specific, regenerable)
.metadata/
.recommenders/
.settings/
*.launch
# NOTE: .project and .cproject ARE tracked — they encode toolchain config
# and the CubeIDE-managed makefile layout that the build depends on.

# OS-specific
.DS_Store
.osx.project
Thumbs.db

# Editor
*.swp
*.swo
*~

# CubeMX backup files (left behind after regen)
*.bak
*_save
*.backup
